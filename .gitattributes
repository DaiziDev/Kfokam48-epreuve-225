# Fichiers lus par Docker ou exécutés dans des conteneurs Linux : toujours LF,
# même sur un clone Windows (un \r casse les continuations \ du Dockerfile).
Dockerfile text eol=lf
*.dockerignore text eol=lf
docker-compose.yml text eol=lf
*.conf text eol=lf
*.sh text eol=lf
